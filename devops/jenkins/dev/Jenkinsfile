pipeline {
    agent any

    tools {nodejs "NodeJS_14"}

    stages {
        stage('Preparation') {
            steps {
                echo 'Hello from Github';
                sh 'node -v';
                sh 'npm -v';
            }
        }
    }
}
