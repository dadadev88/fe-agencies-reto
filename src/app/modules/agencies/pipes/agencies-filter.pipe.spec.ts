import { AgenciesFilterPipe } from './agencies-filter.pipe';

describe('AgenciesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AgenciesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
